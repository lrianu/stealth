from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='Track fish in dam passage windows',
    author='lrianu',
    license='',
)
